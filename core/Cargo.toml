[package]
name = "kb-core"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
# Единственная зависимость ядра: sqrt/atan2/sin для no_std (физика и ИИ
# не имеют доступа к std-математике). Чистый Rust, ноль транзитивных депов.
libm = "0.2"
